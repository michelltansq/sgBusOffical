import React from 'react';
import {View, Text} from 'react-native';

const busStop = ({navigation}) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Bus Stop!</Text>
    </View>
  );
};

export default busStop;
